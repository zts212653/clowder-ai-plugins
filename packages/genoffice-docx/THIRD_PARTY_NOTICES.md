# Third-party notices

The renderer artifact is built from GenOffice `v0.8.1039`, commit
`e833fff87f5628cc681e0fd1a063ce64fde5baa4`, under Apache License 2.0. The exact
archive, lockfile, LICENSE, and NOTICE digests are recorded in `source-lock.json`.

The enterprise `ee/` tree is excluded before extraction and is never included in
the renderer or npm package. The Electron main/preload applications are neither
built nor packaged.
