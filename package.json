{
  "private": true,
  "name": "@clowder-ai/plugins-workspace",
  "description": "Clowder AI plugin ecosystem — contract, SDK, and reference plugins",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "test:fresh-consumer": "node --test scripts/fresh-consumer-install.test.mjs",
    "test:train-c1-inventory": "node --test scripts/train-c1-inventory.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "conformance": "pnpm --filter @clowder-ai/plugin-contract conformance",
    "catalog:check": "pnpm --filter @clowder-ai/plugin-contract build && pnpm --filter @clowder-ai/video-analysis build && pnpm --filter @clowder-ai/video-generation build && pnpm --filter @clowder-ai/wechat-visible-reader build && pnpm --filter @clowder-ai/weixin-mp build && pnpm --filter @clowder-ai/plugin-sdk build && pnpm --filter @clowder-ai/companion build && node --test scripts/catalog-static-package.test.mjs && node scripts/catalog-check.mjs"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  }
}
