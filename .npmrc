engine-strict=true
auto-install-peers=true
link-workspace-packages=true
# Cat processes inherit NODE_ENV=production, which makes pnpm skip
# devDependencies and later typechecks fail spuriously. Pin production=false
# so installs are independent of the caller's environment.
production=false
